#pragma once
#include "gameDefinitions.hpp" // Se precisar de algo de lá

// --- Protótipos das funções ---
void drawDump(int); 
void drawTrashBin(float r, float g, float b, const char* label, float x, float z);

#include <string>